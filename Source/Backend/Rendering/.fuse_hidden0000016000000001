#pragma once

#include "Texture_data_adapter.hpp"
#include <gli/gli.hpp>

namespace rendering
{

class GLI_texture_data_adapter : public Texture_data_adapter
{

public:

	GLI_texture_data_adapter(const Texture_description& description, const gli::texture2D& texture);
	virtual ~GLI_texture_data_adapter();

	virtual bool get_slice(Texture_description::Data& data, uint32_t index) const;

private:

	gli::texture2D m_texture;

public:

	static Data_format::Value map(gli::format gli_format);
};

}


